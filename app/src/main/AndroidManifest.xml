<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.phanmemquanlynhansu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SuaNhanVienActivity"></activity>
        <activity android:name=".SuaCuaHangActivity" />
        <activity android:name=".ChucVuActivity" />
        <activity android:name=".SuaChucVuActivity" />
        <activity android:name=".CuaHangActivity" />
        <activity android:name=".SuaCaLamActivity" />
        <activity android:name=".CaLamActivity" />
        <activity android:name=".NhanVienActivity" />
        <activity android:name=".ThemNhanVienActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>